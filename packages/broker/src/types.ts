// Package-level shared types are reserved for Phase 2+. Phase 1 keeps the
// public type surface inside `session-store/types.ts` and re-exports it via
// the package barrel `index.ts`.
export type {};
